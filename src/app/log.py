""" This class handles the logging of server execution, and especially periodic logging of the Merkle tree state outside of the container for recovery if the system crashes"""

import logging


class LogHandler:
    pass